My Collection of Opensource Repositories

# Add a new repository
```
git submodule add -b <branch> <url> <my desired folder>
git remote add upstream https://github.com/<url>

```

# Update a repository
```
git fetch upstream
git rebase upstream/master

```
